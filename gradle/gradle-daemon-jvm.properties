# Use any locally installed JDK 21 (Android Studio JBR, Temurin, etc.).
# Avoid pinning Amazon Corretto + Foojay auto-download, which hangs when
# Corretto is not installed and another Gradle process holds the JDK lock.
toolchainVersion=21
